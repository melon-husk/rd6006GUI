{
  "name": "newdongdong",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "fixall": "npm i -D electron-rebuild && rm -r node_modules && rm -r package-lock.json && npm i && ./node_modules/.bin/electron-rebuild"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.0.0",
    "electron-rebuild": "^1.11.0"
  },
  "dependencies": {
    "dialogs": "^2.0.1",
    "jsmodbus": "^4.0.3",
    "serialport": "^9.0.0"
  }
}
